export type Experience = {
  name: string;
  timeline: string;
  description: string;
};

export type Social = {
  id: string;
  url: string;
  text: string;
};

export type Project = {
  name: string;
  desc: string;
  src: string;
  url: string;
  tech: string[];
};
